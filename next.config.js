/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains : ["images.pexels.com", "i.imgur.com", "stylearena.net", "placeimg.com", "api.lorem.space", "picsum.photos"]
    }
}

module.exports = nextConfig
